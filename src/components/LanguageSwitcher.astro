---
import { getLangFromUrl } from '../i18n/utils';
import { languages } from '../i18n/ui';

const lang = getLangFromUrl(Astro.url);
const currentPath = Astro.url.pathname;

const pathWithoutLang = currentPath.replace(/^\/(es|en)/, '') || '/';
const langEntries = Object.entries(languages);
---

<div class="flex gap-2 md:gap-3 items-center text-center">
  {langEntries.map(([langCode, langName], index) => {
    const href = langCode === 'es' ? `/es${pathWithoutLang}` : `/en${pathWithoutLang}`;
    const isActive = lang === langCode;
    
    return (
      <>
        <a 
          href={href}
          class:list={[
            "text-sm md:text-md rounded hover:text-body text-heading",
            isActive 
              ? "font-bold" 
              : ""
          ]}
          aria-label={`Switch to ${langName}`}
        >
          {langCode.toUpperCase()}
        </a>
        {index < langEntries.length - 1 && <span class="text-md md:text-lg text-heading cursor-default">|</span>}
      </>
    );
  })}
</div>
