---
import { getLangFromUrl } from '../i18n/utils';
const lang = getLangFromUrl(Astro.url);
import '../styles/global.css';
---
<!doctype html>
<html lang={lang} class="scroll-smooth min-h-dvh md:h-dvh bg-linear-to-tl from-gray-50 to-gray-150 dark:from-gray-950 dark:to-gray-900 ">
    <head>
        <script is:inline>
            //Detectar tema del navegador
            if (localStorage.getItem('color-theme') === 'dark' || (!('color-theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
                document.documentElement.classList.add('dark');
            } else {
                document.documentElement.classList.remove('dark')
            }
        </script>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="icon" href="/favicon.ico" />
        <meta name="generator" content={Astro.generator} />
        <title>Daniel Grela</title>
    </head>
    <body class="px-4 md:px-12  min-h-dvh md:h-dvh flex flex-col items-center justify-center text-center md:text-start text-heading">
        <slot />
    </body>
</html>

<style>
    html,
    body {
        margin: 0;
        width: 100%;
    }
</style>